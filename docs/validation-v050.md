# 0.5.0 双语阅读与空闲AI队列验证
日期：2026-09-14。

## 交付内容
中文标题＋英文原题；中文摘要译文、英文原文、中文总结三个独立阅读区。一次性 JACS 订阅迁移、近7天全部待完成项、手动刷新处理、暂停、每日空闲或指定时刻后空闲队列。无新增依赖和工作进程，音乐数据不变。

## 已通过的自动化检查
- Release 发布构建成功，0警告0错误。阅读自动化 73 项通过；详见 [逐项结果](validation-v050-checks.txt)。
- 覆盖旧数据补齐、内容修订、重复点击缓存、模型缓存隔离、无摘要标题翻译、结构异常和截断、超时、全队列限流退避、取消订阅、未知日期、7天范围、一次性迁移、暂停和跨日恢复。
- 空闲条件与时刻边界使用模拟条件验证。暂停后当前云端响应仍保存；云端超时不误判为程序退出；不自动重发结果未知的请求。
- 原有音频信号回归8项通过，包括真实 byte/float 适配器复用、静音、缓冲不足清零，以及总音量/电台音量两组等响对照。音频代码本轮未修改。
- 760/520 DIP 阅读/设置离屏布局以及100%、125%、150%、200%渲染图生成；人工检查双语长标题、正文、设置窄宽度和筛选空间。离屏渲染不能替代真实系统DPI、键盘和鼠标验收。

## 真实 DeepSeek 测试结果：未完成
- 应用独立测试中，4次只读模型目录连接尝试均在取得模型列表前发生传输/握手中断，未发送任何生成 POST：**0/3**。没有实际输入/输出 token 或可报告的缓存命中用量，也没有三篇翻译质量结果。
- 无认证协议诊断曾收到 HTTP/2 401，确认部分连接可达；TLS 1.2 结果并不稳定，因此最终保留系统TLS与证书验证，优先协商HTTP/2。实际带认证的应用请求仍失败；不能据此认定密钥无效，也不能认定 Flash 模型已经验证可用。
- 测试不实例化订阅调度，不修改正式用户AI配置；密钥通过隐藏输入保存为当前用户DPAPI独立临时配置，测试退出后已删除。源码、日志和包不包含密钥。次数保护在发送生成前落盘；没有盲目重试生成。
- [结构化测试记录](validation-v050-live.json) 保留失败原因、零生成次数与临时配置清理状态。

## 资源短测（不是完整音乐验收）
Windows本机发布运行时。下列内存是 **Private Commit**，不是私有工作集；不包含主窗口、桌面组件和音频。单独采样未测真实付费请求。

| 场景 | 时长 | 平均整机CPU | 私有提交峰值 | 阶段增量 |
|---|---:|---:|---:|---:|
| 暂停队列＋空闲监测 | 15.92s | 0.033% | 16.34 MiB | 0.72 MiB |
| 单次模拟请求＋保存 | 15.90s | 0.135% | 19.47 MiB | 3.89 MiB |
| 万条文章查询后的空闲 | 30.37s | 0.017% | 42.86 MiB | 未单列 |

万条缓存测试中，BBC同步约 2.00s，返回25条；100次分页查询平均 44.59ms。没有执行两小时测试，已有音乐性能基线继续保留，不能把此次阅读短测作为整机音乐性能重新通过。

## 待实机验证
- 当前用户网络下真实模型列表、三篇JACS翻译质量（数字、化学式、限定词、术语、总结依据）、真实token及实际服务限流。
- 真实连续3分钟无键鼠＋30秒低CPU、恢复操作后停止下一篇、全屏/演示与节电限制；此次只验证系统适配代码和策略输入，不宣称完成真实状态切换。
- 实际断网恢复、退出后不驻留，100%–200%系统缩放的键盘焦点、复制、返回位置以及长时间人机交互。
- 真实AI流量下音乐持续播放的资源增量；本次只有隔离模块短测和音频信号回归。
