# 静隅 0.3.2：组合直接删除与撤销

“声音场景 → 我的组合”中每个个人组合增加可见的删除按钮，标题右侧提供撤销删除。删除最后一个组合后仍能撤销；无可撤销内容时按钮禁用。删除不改变正在使用的声道和音量，也不影响播放状态。原有内置预设保持保留。

七项针对性回归通过，见 evidence-v032/scene-test.txt：保存与选择、覆盖不重复、重命名、直接触发可见删除按钮、删空后撤销、页面缓存、重启保留与删除持久化。测试在隔离的临时数据目录执行。Release 自包含构建通过；没有重跑性能长测或人工鼠标验收。

退出托盘里的旧版后运行 0.3.2，无需清除用户设置。
