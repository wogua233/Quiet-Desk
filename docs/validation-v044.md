# 0.4.4 订阅筛选与文章排序
日期：2026-09-14。

- 默认来源为“订阅刊物”，SQL查询实时匹配已勾选来源。取消订阅不删除文章。
- 保留显式“全部刊物”及单一期刊选项；收藏筛选同样遵守来源范围。
- 默认按时间、更新在前；支持更老在前和期刊名A–Z。排序跟随发表／发现日期选项，未知日期置后，同日使用发现时间和唯一ID稳定排序。
- 排序先于分页，避免每页自行排序。期刊名排序下时间方向控件禁用，切回时间保留原方向。
- 39项阅读自动化检查通过，新增覆盖取消订阅、显式全部刊物、时间新旧、刊物名、默认状态和最新日期范围。
- 发布构建通过（0警告0错误）；760/520 DIP及多尺度离屏布局生成，检查了默认筛选与排序控件。离屏布局不等同于真实键盘／系统DPI验收。
- 无新增后台轮询，不改变音乐、摘要服务或每日额度；未重跑音频长测，未调用付费API。
